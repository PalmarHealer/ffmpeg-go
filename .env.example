PORT=50051
TOKEN=change-me
MAX_JOBS=4

# Set to true when the media directory is mounted at the same path on both
# the client host and the server container (e.g. /videos).
# FFmpeg will read the input file directly instead of streaming it over gRPC.
DIRECT_INPUT=false
